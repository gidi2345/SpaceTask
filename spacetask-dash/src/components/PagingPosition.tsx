import React , {usestate, useeffect} from 'react';
import {Action} from '../types/action.interface';

interface PagingPositionProps {
    actions: Action[] | any;
}
//todo: need to implement
const PagingPosition = (props: PagingPositionProps) => {
   

    return (
        <div></div>
    )
}

export default PagingPosition;
